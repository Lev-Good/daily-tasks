# שינויים

## v1.4.10

- **תיקון — התוכנה לא נפתחת אצל חלק מהמשתמשים, בלי שום הודעה**: שני משתמשים דיווחו שההתקנה עוברת בהצלחה אבל התוכנה לא נפתחת — לא חלון, לא סמל בשורת המשימות, לא מתפריט התחל, ולא עזר השהיית האנטי-וירוס. הבדיקה העלתה שהקובץ שהתפרסם תקין לחלוטין (זהה לקוד המקור), והבעיה הייתה שהתוכנה **נכשלה בשקט** בכל מסלולי הכשל ולכן אי אפשר היה לדעת מה קרה. התיקונים:
  - **קובץ התוכנה נשמר כעת ב-UTF-8 עם BOM**: קובץ `.ps1` בלי BOM נקרא ב-Windows PowerShell 5.1 לפי קידוד ה-ANSI של המערכת. במחשבים שה-ANSI שלהם אינו UTF-8 — כלומר ברוב הגדרות ווינדוס — כל הטקסט העברי שבקובץ, ובכלל זה ה-XAML של החלון, נקרא כג'יבריש וטעינת החלון נכשלת מיד. במחשב שבו פותחה התוכנה מופעלת תמיכת ה-UTF-8 של ווינדוס, ולכן התקלה לא הופיעה שם.
  - **כל שגיאה של התוכנה גלויה כעת**: ה-Launcher כבר לא מתעלם משגיאות ההרצה — הוא רושם אותן ל-`launcher.log` ומציג למשתמש חלון שגיאה בעברית עם נתיב הלוג, במקום לצאת בשקט.
  - **"התוכנה כבר רצה" לא שותק יותר**: אם מופע קודם תקוע או לא מגיב, ה-Launcher ממתין לאישור (ACK) ממנו; אם אין אישור — מוצגת שאלה אם לסגור אותו ולפתוח מחדש, והתוכנה עושה זאת בעצמה. עד היום הלחיצה על קיצור הדרך פשוט לא עשתה כלום במקרה כזה.
  - **בדיקת דרישת מערכת**: אם Windows PowerShell 5.1 חסר או פגום במחשב, מוצגת הודעה ברורה עם הוראות התקנה (ווינדוס 11 מאפשר להסיר אותו).
  - **המתקין לא מדלג יותר על קבצים נעולים בשקט**: אם התוכנה רצה בזמן ההתקנה, קובץ ההפעלה לא הוחלף (נעול) והמשתמש נשאר עם גרסה מעורבת בלי לדעת. המתקין סוגר כעת את המופע הרץ לפני ההעתקה, מאמת שכל קובץ נכתב בשלמותו, ורושם הכל ל-`install.log`; אם משהו בכל זאת לא הוחלף — מוצגת הודעה מפורשת.
- **תיקון — שמירת המשימות**: הכתיבה האטומית של `tasks.json` נכשלה בכל שמירה (`File.Replace` עם `$null` הומר למחרוזת ריקה ולכן חזר השגיאה "הנתיב אינו בעל צורה חוקית"), ונותרו קבצי `tasks.json.tmp`. כעת השמירה עובדת כפי שתוכננה, עם נסיגה בטוחה אם ההחלפה האטומית אינה אפשרית.
- **כלי אבחון חדש — "בדיקת תקינות"**: קיצור דרך חדש בתפריט התחל (וגם `diagnose.cmd` בתיקיית ההתקנה) שמייצר דוח מלא לקובץ `diagnose.txt` — גרסת ווינדוס ו-PowerShell, קידוד ה-ANSI של המערכת, נוכחות המנוע של PowerShell 5.1, מצב המופע הרץ, רשימת הקבצים והחתימות שלהם, חסימת הורדה (Zone.Identifier), קיצורי הדרך, ההגדרות ויומני השגיאות. כך אפשר לאבחן תקלה במחשב אחר בלי ניחושים.
- **תיעוד שגיאות מורחב**: בנוסף ל-`error.log` נרשמים מעתה גם `launcher.log` (הפעלה, מופע קיים, שגיאות הרצה) ו-`install.log` (מה הועתק, מה נכשל).

## v1.4.9

- **תיקון — לחיצה על קיצור הדרך בשולחן העבודה או בתפריט התחל פתחה את התוכנה מוסתרת**: כשהגדרת "הפעלה ממוזערת" פעילה, כל לחיצה ידנית על קיצור הדרך (או לחיצה כפולה על הקובץ) הפעילה את התוכנה ישירות למגש המערכת — בלי חלון על המסך, ורק לחיצה על האייקון ליד השעון חשפה אותו. עכשיו הפתיחה הידנית מציגה תמיד את החלון, ורק הפעלה אוטומטית עם Windows (קיצור ההפעלה האוטומטית) מכבדת את "הפעלה ממוזערת":
  - ה-Launcher (`DailyTasks.exe`) מעביר לסקריפט `--show` בכל פתיחה ידנית, כך שהחלון מוצג תמיד גם כשההפעלה הממוזערת מוגדרת.
  - קיצור ההפעלה האוטומטית (Startup) מקבל כעת `--autostart` ושומר על ההתנהגות הקודמת — התחלה שקטה במגש אם כך הוגדר. קיצורי Startup קיימים שגויים מתוקנים אוטומטית בהפעלה הבאה.
  - שיפור אמינות בהעלאת החלון לראש המסך כשהתוכנה כבר רצה.

## v1.4.8

- **תיקון באג — לחיצה על האיקס בהודעת ההתראה לא סגרה אותה**: כפתור הסגירה (✕) בהודעה הקופצת נתלה על המשתנה המקומי של החלון (`$win`) בתוך ה-handler — סגירה מסוג זה ב-PowerShell אינה אמינה, ולכן במקרים רבים הלחיצה לא עשתה דבר. הכפתור עבר לאותו מנגנון שבו משתמשים כל הכפתורים הפועלים בהודעה (שליפת החלון מ-`$this` בזמן הלחיצה). בנוסף, לחיצה על כל אזור בהודעה שאינו כפתור פעולה (כמו אזור הכותרת או שורת הטקסט) סוגרת עכשיו גם היא את ההודעה — כך שההודעה תמיד ניתנת לסגירה.
- **קרדיט בתפריט ההגדרות**: בתחתית תפריט ההגדרות נוספה שורת קרדיט — "נוצר על ידי **לב טוב דיגיטל**", לחיצה עליה פותחת את אתר הבית: https://digital.levtov.uk/
- **משימת פתיחה לדוגמה**: בהפעלה הראשונה (כשאין משימות בכלל) התוכנה מוסיפה משימה אחת לדוגמה — "הצטרפות לפורום העורכים התורניים" עם הקישור https://editorforum.levtov.uk/ — כדי להראות איך נראית משימה. המשימה שקטה (ללא צליל), מתוזמנת ל-30 דקות מהיצירה, ונוספת רק פעם אחת אי-פעם (גם אם מוחקים אותה אחר כך, היא לא חוזרת; משתמש קיים עם משימות לא מקבל אותה).

## v1.4.7

- **תיקון באג חמור — התראה לא נשמעה אחרי שינוי שעה**: כשמשימה עבר זמנה (או דולגה בהתחלה) ואז ערכו את שעתה/תאריכה/התזכורת לזמן עתידי, ההתראה החדשה לא נשמעה כי המשימה עדיין סומנה כ"כבר הודעה היום". עכשיו עריכת שעה/תזכורת/תאריך מאפסת את מצב ההתראה של המשימה, וההתראה תישמע בזמן החדש.
- **בחירת צליל ההתראה**: 13 צלילים מרגיעים שנוצרים מקומית (בלי קבצים חיצוניים) בתפריט ההגדרות, עם כפתור תצוגה מקדימה להשמעה/עצירה. **"פעמון עדין" הוא ברירת המחדל החדשה** — הצליל הישן עדיין זמין תחת "ברירת מחדל (ישנה)".
- **עוצמת צליל**: סליידר עוצמה 0–100% בהגדרות, נשמר ב-`settings.json` ומופעל בכל התראה (דרך MediaPlayer במקום SoundPlayer שלא תמך בעוצמה).
- ההגדרות החדשות נשמרות מיד בעת שינוי; הצלילים מיוצרים בהרצה הראשונה לתיקיית `sounds\` ליד התוכנה.

## v1.4.6

- **מצב כהה (Dark Mode)**: ערכת צבעים כהה מלאה ורגועה לכל הממשק — חלון, כרטיסי משימות, חלונות פרטים, הודעות, תפריט ההגדרות וגלגול.
  - **בחירה אוטומטית לפי Windows** כברירת מחדל — התוכנה עוקבת אחרי ערכת הצבעים של מערכת ההפעלה (`AppsUseLightTheme`).
  - **הגדרה ידנית**: בתפריט ההגדרות התחתון נוסף סעיף "ערכת צבעים" עם שלוש אופציות — אוטומטי / בהיר / כהה. הבחירה נשמרת (`settings.json`) ותחול בהפעלה הבאה.
- **הנדסה**: כל צבעי הממשק עברו למערכת אסימוני נושא מרכזית (טוקנים) — שינוי ערכת הצבעים בעתיד ידרוש פלטה אחת במקום עשרות עריכות.

## v1.4.5

- **מהפך עיצובי — מראה נקי ומקצועי יותר, עם אקסנט אינדיגו יחיד ושקט**:
  - **הירו רגוע**: הפאנל העליון איבד את השיפוע הסגול הרווי — עכשיו הוא כרטיס שקט עם רקע ניטרלי, טקסט כהה, אחוזי ההתקדמות והפס באותם גוון אינדיגו בלבד.
  - **מסננים בקבוצה**: ארבעת המסננים (היום/מחר/השבוע/הכל) מקובצים בתוך רצועה אחת ממוסגרת, כשהפעיל מודגש באינדיגו.
  - **כרטיסי משימות מחודשים**: תג החזרה עבר לגוון ניטרלי (במקום סגול), רדיוסים אחידים, וצל רך ומדויק.
  - **מצב ריק וקטורי**: האימוג'י 🎉 הוחלף באייקון סימון שקט תואם עיצוב.
  - **אנימציות קלות**: חלון נכנס בפדיחור+זום עדין, סימון משימה "פועם" בצורה קטנה בעת ההשלמה, וריווח/רדיוסים אחידים.
- **שמירה אטומית**: המשימות וההגדרות נשמרות דרך קובץ זמני + החלפה — סגירה כפויה או קריסה באמצע שמירה לא יכולות עוד להשאיר את `tasks.json` קטוע/ריק; רשימה ריקה נשמרת כ-`[]` תקני.
- **תיקון הודעות חסרות**: הודעות "משימות שהוחמצו" כבר לא נפלות כשהתוכנה מתחילה ממוזערת למגש (בעיית Owner) — הן נפתחות כרגיל.

## v1.4.4

- **תיקון חזותי בהודעות**: הצל של כרטיס ההתראה הוקטן מאוד — הוא כבר לא יוצר "טריז אפור" בפינות המעוגלות של הכרטיס.

## v1.4.3

- **חלון המשימה המפורטת מתחיל ממה שכבר נכתב**: כשכותבים משימה בתיבת הכתיבה המהירה (עם או בלי שעה, למשל "שיחת טלפון 09:30") ולוחצים על כפתור "+" (או Enter), החלון נפתח עם הכותרת והשעה שכבר נכתבו — ומוסיפים עליהן את התדירות, התזכורת ומשימות נוספות באותה שעה. התיבה המהירה מתרוקנת.

## v1.4.2

- **אייקונים מתוקנים**: אייקון התוכנה האמיתי מופיע בראש החלון ליד הכותרת (במקום גליף שבור), והאייקון בשורת המשימות נבנה נכון.
- **הגדרות בתפריט נפתח**: שלוש ההגדרות (הפעלה עם ווינדוס, התחל ממוזער, הודעות במסך מלא) עברו לכפתור גלגל שיניים עם תפריט נפתח — חוסך מקום בתחתית החלון.
- **מקלדת**: כפתור הוספת המשימה המפורטת נגיש עם TAB (עם סימון פוקוס) ונפתח עם Enter/Space.
- **אייקון הפלוס הוקטן** בכפתור ההוספה.
- **חלון המשימה המפורטת**: שדה התיאור הוסר; המשימה הנוכחית מוצגת בראש החלון, ומתחתיה מיד האופציה להוספת משימה נוספת באותה שעה.
- **צליל מאומת**: בדיקה שצליל ההתראות מתנגן והכפתור שולט בו.

## v1.4.1

- **תיקון אזהרת אנטי-וירוס**: ה-Launcher (`DailyTasks.exe`) הוחלף מגרסה שהריצה `powershell -ExecutionPolicy Bypass` כתהליך נפרד לגרסה שמריצה את התוכנה בתוך התהליך עצמו (PowerShell runspace) — בלי תהליך powershell.exe, בלי דגל Bypass, בלי חלון נסתר.
- **המתקין נכתב מחדש ב-C# טהור**: כבר לא מחלץ קבצים ל-`%TEMP%` ולא מריץ סקריפטים — כותב ישירות לתיקיית ההתקנה, יוצר קיצורי דרך ושומר על המשימות הקיימות. זה מסיר את חתימת ה"dropper" שדפנדר זיהה כשגוי.
- לשני הקבצים נוספו: מטא-דאטה של גרסה (1.4.1), אייקון, מניפסט (asInvoker + DPI) ומפרסם.

## v1.4.0

- **הגנה על הנתונים**: המתקין כבר לא דורס `tasks.json` קיים בעדכון/התקנה חוזרת, והתקנה רעננה מגיעה עם רשימת משימות ריקה (בלי משימת בדיקה).
- **יציבות**: טיפול בטוח בשעות לא תקינות (`TryParse`), עדכון הספירה "בעוד X דקות" ללא קפיצות גלילה, מניעת דליפת זיכרון של נגני הצליל, שמירת היסטוריית ההשלמה המלאה (הרצף לא נקטע ב-60 יום), ולוג שגיאות (`error.log`).
- **מסך מלא**: הודעות לא קופצות מעל חלון במסך מלא כברירת מחדל, עם אופציה בהגדרות להציג אותן בכל זאת.
- **הודעות**: טוסט "משימות שהוחמצו" בפתיחה, טוסטים במסך שבו נמצא העכבר, ואימוג'ים מוסתרים בהודעה שנחשפים בריחוף עכבר.
- **ממשק**: פילטרים היום/מחר/השבוע/הכל, חיפוש משופר (Ctrl+F, Esc, כפתור ניקוי), גרירה לסידור מחדש, משימה שהושלמה מסומנת בקו חוצה ועמעום, "התחל ממוזער למגש", שדה תיאור אופציונלי, תיקון אייקונים חתוכים, וי חלש שהופך לירוק, DPI חד במסכים בקנה מידה גבוה.
- **התקנה**: תיקון כפתורי עיצוב, גיבוי `DailyTasks.old.ps1` לפני עדכון, התקנה שלא נופלת כשהתוכנה רצה, ומסיר התקנה (`uninstall.cmd`).

## v1.2.0

- אייקון .ico קבוע: בסרגל המשימות, במגש ובקיצורי הדרך.
- לחיצה על הקיצור כשהתוכנה כבר רצה פותחת את החלון הקיים (ללא כפילות).
- המתקין מפעיל את התוכנה אוטומטית ורק אם היא לא רצה כבר.

## v1.0.0

- גרסה ראשונה: תזכורות משימות יומיות בעברית (PowerShell + WPF), התראות, צלילים, חזרות וסנכרון.
